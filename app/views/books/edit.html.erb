<h4>Editing Book</h4>

<%= form_for(@book) do |f| %>
<h5>Title</h5>
<%= f.text_field :title %>

<h5>Body</h5>
<%= f.text_area :body %><br>

<%= f.submit 'Update Book' %><br>

<%= link_to "Show",book_path(@book) %>|
<%= link_to "Back",books_path %>
<% end %>